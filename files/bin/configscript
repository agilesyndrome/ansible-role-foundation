#!/bin/bash
set -e
TEMPLATE=${1}

if [ -z "${TEMPLATE}" ] then;
    echo "Usage: ${0} template_name"
    exit 1
fi

if [ ! -f ~/config/${TEMPLATE} ] then;
    echo "~/${TEMPLATE} file does not exist"
    exit 1
fi

~/config/${TEMPLATE} | configfile ssm | bash >> ~/log/image.log